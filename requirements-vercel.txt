# Requirements ottimizzati per Vercel (senza dipendenze pesanti)
# Le dipendenze ML sono caricate solo quando necessarie

# Core dependencies
requests>=2.31.0
python-dotenv>=1.0.0
pyyaml>=6.0
beautifulsoup4>=4.12.0
lxml>=4.9.0
readability-lxml>=0.8.1
feedparser>=6.0

# LLM (scegli uno o entrambi)
openai>=1.0.0
anthropic>=0.7.0

# Utilities
python-dateutil>=2.8.0
urllib3>=2.0.0
markdown>=3.4.0

# Optional: per miglior parsing HTML
html5lib>=1.1

# Frontend mock server
flask>=3.0.0
flask-cors>=4.0.0

# NOTE: sentence-transformers e numpy sono esclusi per Vercel
# Verranno installati solo se necessario durante runtime
# Il codice gestisce l'assenza di queste dipendenze
